<%- include("partials/header"); -%>

<div class="container mb-5 mt-5">
  <p class="section-subtitle my-4 pb-5">Reggiseni</p>
  <div class="row">

    <% for (let i = 0; i<8; i++) { %>

    <div class="col-lg-3 col-md-4 mb-5">
      <div class="card mb-2">
        <div class="productCard align-items-center m-0 text-center">
          <img src="<%= foundItems[i].picURL %>" class="rounded-bottom" width="180" height="180">
          <h5 class="mt-3 text-uppercase"><%= foundItems[i].name %></h5>
          <div class="m-2 card-text info">

            <span class="text1 d-block">Materiale: <%= foundItems[i].material %></span>
          </div>
          <p class="m-1"> Taglia: <%= foundItems[i].size %></p>
          <p class="m-1"><strong> <%= foundItems[i].price %>€ </strong> </p>

        </div>
        <div class="cartBar  text-center mx-auto  m-2 cursor">
          <button type="submit" name="button" class="btn btn-link text-uppercase" id="<%= foundItems[i].name %>">Aggiungi al carrello</button>
         </div>
      </div>
    </div>

    <%  } %>

  </div>

</div>
